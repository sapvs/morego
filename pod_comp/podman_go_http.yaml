# Generation of Kubernetes YAML is still under development!
#
# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#
# Created with podman-1.9.3
apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: "2020-10-30T12:03:44Z"
  labels:
    app: gohttp
  name: gohttp
spec:
  containers:
  # Go container
    - env:
        - name: HOSTNAME
          value: go_http_1
        - name: container
          value: podman
      image: localhost/go_http:0.1
      name: go_http_1
      ports:
        - containerPort: 8081
          hostPort: 8081
          protocol: TCP
      resources: {}
      securityContext:
        allowPrivilegeEscalation: true
        capabilities: {}
        privileged: false
        readOnlyRootFilesystem: false
        seLinuxOptions: {}
      workingDir: /
  # Go container 2 // to be Rabiitmq container soon
    - env:
        - name: HOSTNAME
          value: go_http_2
        - name: container
          value: podman
      image: localhost/go_http:0.1
      name: go_http_2
      ports:
        - containerPort: 8081
          hostPort: 8082
          protocol: TCP
      resources: {}
      securityContext:
        allowPrivilegeEscalation: true
        capabilities: {}
        privileged: false
        readOnlyRootFilesystem: false
        seLinuxOptions: {}
      workingDir: /
status: {}
